import 'package:flutter/widgets.dart';

/// Profile avatar size
const double kAvatarLarge = 128.0;

/// Default list item leading image
const double kListTileImageSize = 48.0;

/// Default padding value in application
const double kContainerPaddingValue = 16.0;

/// Default container padding
const kContainerPadding = EdgeInsets.all(kContainerPaddingValue);

/// Small circular progress indicator size
const double kProgressIndicatorSmall = 32.0;

/// Middle circulat progress indicator size
const double kProgressIndicatorMiddle = 48.0;

/// Large circular progress indicator size
const double kProgressIndicatorLarge = 72.0;

/// Default card corners radius
const double kCardCornerRadius = 8.0;

/// Default card border radius
const kCardBorderRadius = BorderRadius.all(Radius.circular(kCardCornerRadius));

/// Common card image height
const double kCommonCardImageHeight = 150;

/// Image card height
const double kImageCardHeight = 240;
